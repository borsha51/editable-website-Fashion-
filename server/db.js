import{createClient}from'@libsql/client';export function getDb(){const url=process.env.TURSO_DATABASE_URL,authToken=process.env.TURSO_AUTH_TOKEN;if(!url||!authToken)throw new Error('Turso is not configured');return createClient({url,authToken})}export async function initDb(){const db=getDb();await db.batch([{sql:'CREATE TABLE IF NOT EXISTS orders (id INTEGER PRIMARY KEY AUTOINCREMENT,customer_name TEXT NOT NULL,email TEXT NOT NULL,phone TEXT,address TEXT NOT NULL,items_json TEXT NOT NULL,total INTEGER NOT NULL,status TEXT NOT NULL DEFAULT \'pending\',created_at TEXT NOT NULL)',args:[]},{sql:'CREATE TABLE IF NOT EXISTS subscribers (id INTEGER PRIMARY KEY AUTOINCREMENT,email TEXT UNIQUE NOT NULL,created_at TEXT NOT NULL)',args:[]},{sql:'CREATE TABLE IF NOT EXISTS store_content (id INTEGER PRIMARY KEY CHECK(id=1),data_json TEXT NOT NULL,updated_at TEXT NOT NULL)',args:[]},{sql:'CREATE TABLE IF NOT EXISTS reviews (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,review TEXT NOT NULL,image TEXT,rating INTEGER NOT NULL DEFAULT 5,created_at TEXT NOT NULL)',args:[]}],'write')}